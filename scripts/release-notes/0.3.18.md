NeatWebApp macOS 0.3.18

- 退出主程序时会连带结束它名下的网页应用进程，不再留下孤儿窗口；重启后网站登录态与收藏不受影响。
- 浏览器窗口可以故意摆到屏幕边缘之外，不再被弹回；窗口很大一部分看不见时仍会自动收进侧边栏。
- 收藏当前页与打开收藏列表换成两个一眼能分清的按钮；下载完成提示可以关掉，关掉只关提示、不删文件。
- 精简注入网页的取色脚本，去掉重复计算与多余定时器；长页面卡顿先上了这部分，花大钱的根治继续用诊断数据说话。
- 目录与设置主窗重画：左侧导航 + 右侧详情，应用列表支持拖拽排序（松手才保存，中途取消不算数），设置行对齐整理。
